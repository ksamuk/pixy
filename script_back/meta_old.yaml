package:
  name: pixy
  version: "0.1"

source:
  folder: src

requirements:
  build:
    - python >=3
    - nuitka
    - scikit-allel
    - numpy >=1.15
    - numcodecs
    - pandas
    - tqdm
    - argparse

  run:
    - python >=3
    - nuitka
    - scikit-allel
    - numpy >=1.15
    - numcodecs
    - pandas
    - tqdm
    - argparse

test:
  imports:
    - scikit-allel

about:
  home: https://github.com/ksamuk/pixy